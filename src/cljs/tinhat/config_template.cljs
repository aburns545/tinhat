(ns tinhat.config-template)

; A way to set up faux-environment variables, rename this to config.cljs and
; populate the indicated fields fields

; URL for your deployed api
(def api-url "")


; The name of the DynamoDB table you will be using.
(def table-name "")

(def contact-table-name "")